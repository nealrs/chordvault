# Run lint-staged (backend + frontend lint on staged files)
pnpm exec lint-staged

# Run frontend typecheck and unit tests
cd frontend && pnpm exec tsc --noEmit && pnpm test

# Run backend unit tests
cd .. && pnpm test
